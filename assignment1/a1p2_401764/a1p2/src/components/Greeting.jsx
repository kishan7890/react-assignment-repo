// src/components/Greeting.jsx

import React from "react";

function Greeting({ message }) {
  return (
    <div>
      <h2>{/* complete the missing code */message}</h2>
    </div>
  );
}

export default Greeting;
