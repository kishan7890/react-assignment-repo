// src/components/ProfileCard.jsx

import React from "react";

function ProfileCard({ name, age, location }) {
  return (
    <div>
      <h3>{/* complete the missing code */name}</h3>
      <p>Age: {/* complete the missing code */age}</p>
      <p>Location: {/* complete the missing code */location}</p>
    </div>
  );
}

export default ProfileCard;
